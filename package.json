{
  "name": "foundationx",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "nx run-many --target=build --all",
    "lint": "pnpm exec nx run-many --target=lint",
    "tsc": "pnpm exec nx run-many --target=tsc",
    "validate": "pnpm format:write && pnpm tsc && pnpm lint",
    "format": "prettier \"**/*.{js,ts,tsx,json,md}\" --ignore-path .gitignore",
    "format:check": "pnpm format --check",
    "format:write": "pnpm format --write",
    "prepare": "husky install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nx/eslint": "18.2.3",
    "@nx/jest": "18.2.3",
    "@nx/next": "18.2.3",
    "eslint": "~8.57.0",
    "husky": "^8.0.0",
    "jest": "^29.4.1",
    "lint-staged": "^15.2.2",
    "nx": "18.2.3",
    "prettier": "^3.0.0"
  },
  "dependencies": {
    "next": "14.0.4",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  }
}
